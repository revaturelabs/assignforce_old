export default (tableModule) => {

    tableModule.config(($animateProvider, $httpProvider, $logProvider) => {
        $httpProvider.defaults.useXDomain = true;
        $logProvider.debugEnabled(true);
        $animateProvider.classNameFilter(/\banimated\b/);
    });
}
